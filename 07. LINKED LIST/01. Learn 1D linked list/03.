class Solution {
public:
    int getLength(ListNode* head) {
        if(head == NULL){
            return 0;
        }
        ListNode* temp = head;
        int i = 0;
        while(temp != NULL){
            i++;
            temp = temp->next;
         
        }
        return i;
    }
};
