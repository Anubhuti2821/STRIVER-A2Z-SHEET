/*
Problem: Check if a Number is Prime

Approach:
- If the number is less than or equal to 1, it is not prime.
- Iterate from 2 to n-1.
- If any number divides n completely, return false.
- If no divisor is found, return true.

Time Complexity: O(N)
Space Complexity: O(1)
*/

class Solution {
public:
    bool isPrime(int n) {
        if (n <= 1) {
            return false;
        }

        for (int i = 2; i < n; i++) {
            if (n % i == 0) {
                return false;
            }
        }

        return true;
    }
};

/*
Possible Optimization:
- Instead of checking all numbers from 2 to n-1, check only up to √n.
- If no divisor exists up to √n, the number is prime.

Optimal Time Complexity: O(√N)
Space Complexity: O(1)
*/
