| Problem                | Formula         |
| ---------------------- | --------------- |
| Lower Bound            | First `>= x`    |(it shows the first occurence of any element)
| Upper Bound            | First `> x`     |(it shows the element just after the last occurnce of that element)
| Search Insert Position | Lower Bound     |
| First Occurrence       | Lower Bound     |
| Last Occurrence        | Upper Bound - 1 |
| Ceil                   | Lower Bound     |
| Floor                  | Last `<= x`     |

 int target = *min_element(nums.begin(), nums.end()); to find min element in vector
*max_element(nums.begin(), nums.end());
